
export const metadata = {
  title: "Sign up | Local Store",
  description: "Local Store sign up page",
};

export default function SignupLayout({children}:{children: React.ReactNode}) {
    return (
        <>
            {children}
        </>
    )
}