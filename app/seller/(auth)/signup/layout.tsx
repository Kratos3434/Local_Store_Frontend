export const metadata = {
  title: "Local Store | Seller Sign up",
  description: "Local Store seller sign up page",
};

export default function SellerSignupLayout({children}:{children: React.ReactNode}) {
    return (
        <>
            {children}
        </>
    )
}