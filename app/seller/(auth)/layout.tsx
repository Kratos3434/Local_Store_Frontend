
const SellerAuthRootLayout = ({children}: {children: React.ReactNode}) => {
    return (
        <main className="px-2">
            {children}
        </main>
    )
}

export default SellerAuthRootLayout;