export const metadata = {
  title: "Local Store | Seller Sign in",
  description: "Local Store seller sign in page",
};

export default function SellerSigninLayout({children}:{children: React.ReactNode}) {
    return (
        <>
            {children}
        </>
    )
}