export const metadata = {
  title: "Verify Seller | Local Store",
  description: "Local Store verify seller page",
};

export default function SellerVerifyLayout({children}:{children: React.ReactNode}) {
    return (
        <>
            {children}
        </>
    )
}