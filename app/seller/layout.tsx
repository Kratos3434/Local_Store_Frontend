export const metadata = {
  title: "Local Store | Seller",
  description: "Become a seller and start selling now!",
};

export default function SellerLayout({children}:{children: React.ReactNode}) {
    return (
        <>
            {children}
        </>
    )
}